def planner_agent(goal):

    return {
        "agent": "planner",
        "steps": [
            f"Analyze goal: {goal}",
            "Generate execution strategy",
            "Prepare execution patch"
        ]
    }

